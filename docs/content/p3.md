# Semana 3 - Ciclos (bucles) en JavaScript  

---

## ¿Qué es un ciclo?
Un **ciclo** (o **bucle**) es una estructura que **repite un bloque de código** mientras una condición sea verdadera.  
En JavaScript disponemos de tres estructuras básicas:

- `for`
- `while`
- `do...while`

!!! tip "Flujo conceptual"
    ```mermaid
    flowchart TD
        A([Inicio]) --> B{¿Condición<br>verdadera?}
        B -->|Sí| C[Ejecutar bloque]
        C --> B
        B -->|No| D([Fin])
    ```

---

## 1. El ciclo `for`

### 1.1 Sintaxis clásica
```js
for (inicialización; condición; expresión-final) {
    // código a repetir
}
```

- **inicialización**: se ejecuta **una sola vez** al entrar.
- **condición**: se evalúa **antes** de cada iteración.
- **expresión-final**: se ejecuta **después** de cada iteración.

!!! example "Imprimir 0–4"
    ```js
    for (let i = 0; i < 5; i++) {
        console.log(i);   // 0 1 2 3 4
    }
    ```

### 1.2 Variaciones
#### Saltos personalizados
```js
for (let i = 2; i <= 20; i += 2) {
    console.log(i);   // pares hasta 20
}
```

#### Recorrido inverso
```js
for (let i = 10; i >= 0; i--) {
    console.log(i);   // 10 9 ... 0
}
```

#### Declaraciones múltiples
```js
for (let i = 0, j = 10; i < j; i++, j--) {
    console.log(`i=${i}, j=${j}`);
}
```

### 1.3 Omitir partes
```js
let i = 0;
for (;;) {              // equivalente a while(true)
    if (i >= 5) break;
    console.log(i++);
}
```

---

## 2. El ciclo `while`

### 2.1 Sintaxis
```js
while (condición) {
    // código
}
```

!!! danger "Riesgo de bucle infinito"
    Asegúrate de que **la condición se vuelva falsa** en algún momento.

!!! example "Sumar números hasta 10"
    ```js
    let suma = 0;
    let n = 1;
    while (n <= 10) {
        suma += n;
        n++;
    }
    console.log(suma); // 55
    ```

### 2.2 Caso práctico: pedir dato hasta que sea válido
```js
let respuesta;
while (respuesta !== 'si' && respuesta !== 'no') {
    respuesta = prompt('¿Aceptas? (si/no)').toLowerCase();
}
console.log('¡Gracias!');
```

---

## 3. El ciclo `do...while`

### 3.1 Sintaxis
```js
do {
    // código
} while (condición);
```

!!! info "Diferencia clave"
    El bloque se ejecuta **al menos una vez**, porque la condición se evalúa **al final**.

!!! example "Menú repetitivo"
    ```js
    let opcion;
    do {
        opcion = prompt('1-Saludar  2-Salir');
        if (opcion === '1') {
            alert('¡Hola!');
        }
    } while (opcion !== '2');
    ```

---

## 4. Control de flujo dentro de ciclos

| Sentencia      | Efecto                                                  |
|----------------|---------------------------------------------------------|
| `break`        | **Termina** el ciclo de manera inmediata.               |
| `continue`     | **Salta** al siguiente ciclo sin ejecutar el resto.     |
| `return`       | (en funciones) **Sale del ciclo y de la función**.      |

!!! example "break y continue"
    ```js
    // Imprimir solo impares hasta 9
    for (let i = 0; i < 20; i++) {
        if (i % 2 === 0) continue; // salta pares
        if (i > 9) break;          // termina cuando llega a 10
        console.log(i);            // 1 3 5 7 9
    }
    ```

---

## 5. Comparativa rápida

| Característica        | `for`                        | `while`                | `do...while`            |
|-----------------------|------------------------------|------------------------|-------------------------|
| **Evaluación**        | Al inicio                    | Al inicio              | Al final                |
| **Mínimo iteraciones**| 0                            | 0                      | **1**                   |
| **Uso típico**        | Contador conocido            | Condición externa      | Menú, entrada segura    |
| **Legibilidad**       | Todo en una línea            | Más flexible           | Muy claro               |

---

## 6. Casos prácticos con ciclos

### 6.1 Acumulador y promedio
```js
const notas = [7, 5, 9, 10, 6];
let suma = 0;

for (let i = 0; i < notas.length; i++) {
    suma += notas[i];
}

const promedio = suma / notas.length;
console.log(`Promedio: ${promedio}`);
```

### 6.2 Búsqueda con `while`
```js
const claveSecreta = 42;
let intento;

while (intento !== claveSecreta) {
    intento = Number(prompt('Adivina la clave'));
}
alert('¡Acertaste!');
```

---

## 7. Etiquetas (labels) – uso avanzado
Permiten **romper o continuar** ciclos anidados.

```js
outer: for (let i = 0; i < 3; i++) {
    inner: for (let j = 0; j < 3; j++) {
        if (i === 1 && j === 1) break outer; // sale del for exterior
        console.log(`i=${i}, j=${j}`);
    }
}
```

---

## 8. Errores comunes

| Error                                        | Solución                         |
|----------------------------------------------|----------------------------------|
| Olvidar incrementar contador en `while`      | Siempre actualiza la condición   |
| `;` después de `for` o `while`               | Elimina el punto y coma          |
| Variables no inicializadas                  | Declara antes de usar            |

---

## 9. Ejercicios propuestos

!!! question "Ejercicio 1"
    Imprimir la **tabla de multiplicar del 7** (1–10) con `for`.

!!! question "Ejercicio 2"
    Pedir números al usuario hasta que escriba `0`; al final mostrar la **suma total** con `do...while`.

!!! question "Ejercicio 3"
    Recorrer un array de palabras y **detener** la búsqueda cuando encuentres `"fin"` usando `break`.

---

## 10. Recursos adicionales

- [MDN – for](https://developer.mozilla.org/es/docs/Web/JavaScript/Reference/Statements/for)  
- [MDN – while y do...while](https://developer.mozilla.org/es/docs/Web/JavaScript/Reference/Statements/while)  


## 🌀 Taller: 10 Ejercicios de Ciclos en JavaScript  
**Sin funciones, sin recursión, solo `for`, `while` o `do...while`**

Repositorio oficial:  
👉 `https://github.com/tu-usuario/ciclos-sin-funciones`

---

## 📦 Instrucciones de trabajo

1. **Fork** este repositorio.  
2. **Clona** tu fork:
   ```bash
   git clone https://github.com/TU-USUARIO/ciclos-sin-funciones.git
   cd ciclos-sin-funciones
   ```
3. **Crea** los archivos dentro de `/src` exactamente así:  
   - `ejercicio01.js` … `ejercicio10.js`  
4. **Restricciones**:
   - Usa **únicamente** variables, operadores, `console.log`, y ciclos (`for`, `while`, `do...while`).  
   - **No** declares funciones (anónimas o flecha).  
   - **No** uses recursión ni estructuras condicionales como sustitutos de ciclos.
5. **Ejecuta** cada archivo:
   ```bash
   node src/ejercicio01.js
   ```
6. **Commit & push**:
   ```bash
   git add src/ejercicio01.js
   git commit -m "feat: ejercicio 1 resuelto"
   git push origin main
   ```
7. Abre un **Pull Request** al repositorio original cuando termines los 10 ejercicios.

---

## 📁 Estructura esperada

```
ciclos-sin-funciones/
├── README.md
├── src/
│   ├── ejercicio01.js
│   ├── ejercicio02.js
│   ├── ...
│   └── ejercicio10.js
└── .gitignore
```

---

## 🧩 Ejercicios (solo ciclos)

### 1️⃣ Tabla del 9  
**Archivo:** `src/ejercicio01.js`  
Imprime la tabla de multiplicar del 9 del 1 al 10 usando `for`.

---

### 2️⃣ Suma 1 al 1000  
**Archivo:** `src/ejercicio02.js`  
Declara `let total = 0;`.  
Con un ciclo `for`, suma todos los enteros de 1 a 1000 y muestra el resultado.

---

### 3️⃣ Cuenta regresiva con `while`  
**Archivo:** `src/ejercicio03.js`  
Declara `let i = 30;`.  
Con `while`, imprime los números del 30 al 1 (inclusive) en orden descendente.

---

### 4️⃣ Solo pares con `do...while`  
**Archivo:** `src/ejercicio04.js`  
Declara `let n = 0;`.  
Con `do...while`, imprime los números pares del 0 al 50 (inclusive).

---

### 5️⃣ Factorial de 6  
**Archivo:** `src/ejercicio05.js`  
Declara `const limite = 6;` y `let factorial = 1;`.  
Con un ciclo calcula `6!` y muestra el resultado.

---

### 6️⃣ Promedio de un array  
**Archivo:** `src/ejercicio06.js`  
Declara:  
```js
const valores = [12, 8, 15, 9, 6];
let suma = 0;
```  
Recorre el array con un ciclo, calcula la suma y luego imprime el promedio.

---

### 7️⃣ Contraseña “secreto123”  
**Archivo:** `src/ejercicio07.js`  
Declara:  
```js
const clave = 'secreto123';
let intento;
```  
Con `do...while` pide la contraseña por prompt hasta que sea correcta.  
Cuando acierte imprime `"Acceso concedido"`.

---

### 8️⃣ Números primos del 2 al 100  
**Archivo:** `src/ejercicio08.js`  
Usa **dos ciclos `for` anidados** para encontrar e imprimir (uno por línea) los números primos entre 2 y 100.

---

### 9️⃣ Pirámide invertida de asteriscos  
**Archivo:** `src/ejercicio09.js`  
Imprime 5 filas así:  
```
*****
****
***
**
*
```
Usa un solo ciclo `for` para las filas y concatenación de strings.

---

### 🔟 Suma hasta 0  
**Archivo:** `src/ejercicio10.js`  
Declara `let suma = 0;`.  
Con `while` pide números al usuario (usa `prompt`) y súmalos.  
Cuando el usuario escriba `0` (como string o número), imprime la suma total y termina.




